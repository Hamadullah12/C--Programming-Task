#include <iostream>
using namespace std;
int main()
{
    // WHILE LOOP: This is the loop we use when we dont knwo how many times it will iterate
    // 1: While loop will run until its conditions become false .
    // 2: Here we put num = 1 and in condtion num<6 so its true so it will iterate and comes 2 true again but when it reaches to 6
    // and condtion check so it false then it will break the loop

    // int num;
    // cout << "enter num" << endl;
    // cin >> num;
    // while (num < 6)
    // {
    //     cout << "Hello this is hamad." << num << endl;
    //     num++;
    // }
    // cout << "Loop exits as condition becomes true." << endl;

    //2:As the user will get the message again and again until he enter name .epmty is the method . 
    // string name;
    // while (name.empty())
    // {
    //     cout << "What your name?\n";
    //     getline(cin, name);
    // }
    // cout << "Name" << name << endl;
}